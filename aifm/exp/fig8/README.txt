The goal of this experiment is to show the contribution of each offloaded operation.

First, execute the setup_input.sh command in the fig7 folder. Then execute run.sh in every subfolder. The script generates a bunch of log files, and the log format is explained in fig7/README.txt.
