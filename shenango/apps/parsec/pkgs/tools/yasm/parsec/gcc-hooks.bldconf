#!/bin/bash

source ${PARSECDIR}/pkgs/tools/yasm/parsec/gcc.bldconf

# Package dependencies
# NOTE: yasm itself does not depend on hooks but the
# library is added globally and hence we must add it
build_deps="hooks"

